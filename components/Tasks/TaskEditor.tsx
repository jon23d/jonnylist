import { Task } from '@/data/documentTypes/Task';

export default function TaskEditor({ task }: { task: Task }) {
  return (
    <div>
      <h2>Task Editor</h2>
      {/* Task editing form or components will go here */}
    </div>
  );
}
